[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "semgrep"
version = "999.0.0"
description = "Dummy semgrep package for Windows bypass"
dependencies = []
